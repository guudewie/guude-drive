<section class="bg-gray-900 flex flex-grow justify-around max-h-[calc(100vh-3rem)]">
    <div class="flex flex-col gap-5 py-8 px-8 md:px-16 lg:px-32 w-full">
        <div class="flex flex-col h-full">
            <div class="sticky top-0 bg-gray-900 z-0">
                <div class="flex relative items-end justify-between py-4">
                    
                </div>
            </div>

            <!-- Table wrapper with scroll -->
            <div class="overflow-y-auto flex-grow">
                <%- include("./tableShared.ejs")%>
            </div>
        </div>
    </div>
</section>

<%- include("../toastContainer.ejs")%>